<%= form_with(model: listitem) do |form| %>
  <% if listitem.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(listitem.errors.count, "error") %> prohibited this listitem from being saved:</h2>

      <ul>
        <% listitem.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :cart_id, style: "display: block" %>
    <%= form.text_field :cart_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
